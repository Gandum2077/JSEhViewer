# 数据库 v2 实施说明

正式入口是 `src/index.ts`，通过 `database-bootstrap.ts` 显示进度并在 `$thread.background` 中调用 `database-migration.ts`。迁移连接提交并关闭后，回到主线程加载 `src/application.ts`，此时业务模块才会打开 DBManager。`src/database-test.ts` 使用同一后台启动流程。

## 启动与事务

- 全新安装直接得到可用的 v2 数据库，包括 AI 服务预设、10 个收藏分类和全局阅读默认值。
- 版本 0 会先转换旧 AI 配置，版本 1 会补齐此前兼容更新增加的缺失表，再迁移到 v2。准备表、迁移、验证、清理和版本号更新都在同一事务内完成。
- 已有数据库在升级前通过 `VACUUM INTO` 生成 `assets/database.db.before-v2-<UUID>.db`。备份包含已提交的 WAL 内容；备份失败则停止升级。
- 如果已有 `credentials.json`，同时生成 `<数据库备份路径>.credentials.json` 配套备份，任一备份失败都会停止升级。旧数据库备份可能仍含旧凭据，应作为私有恢复资料保留。
- 使用可检查执行结果的 `BEGIN IMMEDIATE` / `COMMIT` / `ROLLBACK`。迁移、验证、旧表清理或提交失败都会回滚，保留原来的版本和数据。
- 验证成功后删除 v1 专用表，保留 `config`、`translation_data`、`banned_uploaders` 和 `favcat_titles`。不在事务内执行 VACUUM。
- 每次启动先在后台检查实际数据库的版本、旧表和汇总视图。正常 v2 启动直接进入应用，不显示迁移等待提示，也不重新迁移、重复备份或覆盖迁移日志。需要初始化或升级时才显示提示。先前独立测试入口生成的 v2 数据库会补齐汇总视图并清理旧表，已有 v2 数据优先。
- 高于 2 的未知版本会被拒绝，不降级、不覆盖。
- 备份、迁移、校验和旧表清理均在后台连接上执行；需要迁移时显示等待提示，完成或失败后才通过 `$thread.main` 返回 UI。中途进度只写阶段日志，避免后台长任务尚未退出时排入主线程 JavaScript 回调，争用共享 JavaScript VM。启动入口不能静态导入业务模块，否则会在等待提示出现前触发同步数据库初始化。
- 每个阶段（SQL 脚本精确到每条语句）执行前更新 `assets/database.db.migration.log`，记录阶段、时间、耗时和状态。成功为 `complete`，捕获到异常为 `failed`，被强制终止会留下 `running` 及最后执行的阶段。日志不含业务数据或绑定参数。失败时显示错误并停止进入业务界面。
- v2 文本 ID 反查旧版图库时，先把待查 ID 转为整数命中旧表主键，再检查文本形式一致，避免对每个标签扫描整个图库表，也避免接受带前导零等非规范 ID。

## 业务读写

- 图库信息、阅读状态、收藏状态、评分和本机下载状态分别写入对应 v2 表。图库完整刷新在一个事务中更新数据，使用 UPSERT，避免 REPLACE 删除父行导致级联丢失。
- `archive_records_v2` 是只读汇总视图，供图库列表、搜索和详情读取；不存在旧表兼容写入入口。业务仍使用数字 gid，绑定到 v2 文本主键时显式转换为字符串。
- 搜索历史与书签使用 sorted_fsearch 作为 ID；搜索词按 term_index 独立存取，保留顺序及分隔符原文。书签以 position_key 持久化排序，当前本地重排在一个事务内重建顺序键。
- AI 和 WebDAV 服务使用内容哈希创建 ID，碰撞时使用 UUID；编辑保留 ID。删除同步实体时保留 deleted 标记。
- 本地标签（旧 tagid 为 0 或 NULL）与网站标签分别存储。网站标签刷新只替换远端缓存；本地标记优先，本地删除标记可隐藏对应网站标签。
- 五个全局阅读参数从旧 config 的 JSON 值转换，缺失项使用 v1 默认值；两个跳页选项默认均为 true。迁移验证后删除旧键，运行期只读写 global_reader_config_v2。
- 图片收藏使用 gid:page_index 作为 ID，并使用软删除。清理阅读历史保留下载图库和有收藏图片的图库；主动删除图库会同步清理其附属状态。
- 下载状态不参与同步。finished=0 表示待恢复任务，finished=1 表示已完成或已取消待恢复。完成时间只在确认下载完成时记录。启动查询不会清空任务表；恢复失败仍可下次重试。

本次实现本地数据库迁移、业务适配与 Cloudflare Worker 网络同步。同步使用 API 版本 1 / 云端结构版本 3，本机 user_version 仍为 2。设置入口为“更多 → 数据库同步”，使用步骤见 [数据库同步指南](app/assets/database-sync.md)。

## 按设备累计标签访问次数

此调整仍属于未发布的本地 v2 迁移。首次初始化在 `config._sync_device_id` 保存随机设备标识，正常重启和同机升级保持不变；它是本机元数据，不参与业务同步，也不能随数据库复制到另一台设备继续使用。

`tag_access_count_v2` 改为每设备、每搜索词一行，ID 为 `device_id:qualifier:namespace:term`，组成部分禁止包含冒号。v0/v1 和之前开发版 v2 的旧计数完整归入当前设备，升级失败则连同设备标识一起回滚。开发版 v2 通过结构检查升级，不增加本地 user_version；迁移完成后仍静默启动。

访问操作只递增本机行。常用标签先按 namespace、qualifier、term 对所有设备的 count 求和，再排序取前十；上传入口只读取本机分量，不能上传这个总和。例如本机 8 次、其他设备 5 次显示 13，本机访问一次后显示 14。

下载合并使用 MAX，重复或乱序响应不重复累加，云端返回较旧的本机分量不会覆盖离线新增次数。完整快照替换其他设备缓存时保留本机分量，非法响应或数据库失败不能导致部分替换。`tag-access-counts.ts` 提供本机分量读取和远端分量合并接口；`src/sync` 已接入 Worker 网络同步。

Worker 对应结构版本为 3（与本地 user_version 分别管理），仅允许设备修改自己的分量，禁止删除/清零计数；解绑设备保留其历史贡献。云端旧模型没有可靠的设备归属，按本次约定在 Worker 升级脚本中清空试验计数，后续由设备上传本机累计值。其他云端业务数据保留，旧同步历史失效后重新完整同步。

## 本机凭据

此次调整直接纳入尚未发布的 v2 迁移，不增加数据库版本号。数据库中的实际配置表名为 `config`：迁移删除其中的 `cookie` 行，新的默认配置也不再创建该行。`webdav_services_v2` 不再包含 `username`、`password` 列，服务 ID 的新建哈希也不包含这两个值。已经运行过开发版迁移的 v2 数据库会重建 WebDAV 表，保留原 ID、顺序、同步版本、删除标记及启用状态。

运行时自动创建 `assets/credentials.json`，正常格式如下：

```json
{
  "version": 1,
  "cookie": "",
  "webdav": {
    "服务 ID": { "username": "用户名", "password": "密码" }
  }
}
```

`cookie` 保持原来的字符串格式，登录模块仍负责 JSON 编解码。WebDAV 凭据按服务 ID 关联，空值可为 `null`；改名或修改连接参数保留原 ID，删除服务时删除对应凭据。配置读取时从文件补齐认证信息，数据库只保存连接定义。AI secure 字段继续使用现有本机敏感配置存储，不进入同步内容。Worker 连接保存在可选的 `sync: { apiUrl, masterKey }` 对象中；不写入数据库。

文件通过 NSData 的原子写入方法替换，避免中途退出留下半份 JSON。为协调 SQLite 与文件，更新期间文件临时包含 `transaction`（新修订号和之前的完整凭据），数据库事务只在 `config` 表的 `_credentials_revision` 键中记录对应的非敏感修订号。提交后清理旧快照；异常时回滚。重启会比较修订号，选择已提交的新内容或未提交前的旧内容；即使清理快照失败，也可以按修订号读取正确值。`_credentials_revision` 是本机恢复元数据，同步时已排除。损坏的凭据 JSON 会阻止启动，不会被空配置覆盖，解析错误也不会输出敏感原文。

普通 v2 启动仍然静默检查，但会检测旧 cookie 行和 WebDAV 敏感列，避免遗漏开发版迁移。凭据文件缺失时按空凭据读取，可以重新登录或填写 WebDAV 认证信息。凭据文件和数据库备份均加入 Git 忽略及安装包排除规则，打包时不会放入默认凭据文件。

## 兼容与验证

沿用迁移计划中对孤立图库附属记录的处理：没有 archives 父记录的标签、单图库设置、图片收藏和下载任务不进入 v2；原记录保留在升级前备份中。搜索记录缺少父项、无效全局设置及派生 ID 冲突会阻止迁移，避免静默覆盖。

运行：

```sh
npm run check:database
node --test scripts/check-gallery-downloader.cjs
tsc --noEmit
npm run build
```

数据库测试需要支持 `node:sqlite` 的 Node.js 22.13 或更新版本以及 tsc。测试编译真实应用模块，通过 JSBox API 适配层在临时 SQLite 文件上运行，覆盖新建、v0/v1/v2、WAL 备份、回滚、72 种阅读配置组合、书签/服务/标签操作、图库刷新、收藏清理和下载状态。还验证反向校验使用主键、后台完成后才加载业务模块、迁移失败不进入应用，以及阶段日志。线程测试使用调度模拟，不能替代 JSBox 真机验证。测试不会打开真实应用数据库。

打包后的脚本仍需在 JSBox 真机上验证首次升级、重启、后台下载恢复及备份文件生成。

## 恢复

验证失败时先保留原库与生成的备份，修正原因后重新启动即可重试。如果强制退出后残留 `database.db-journal`，保留它与对应主库，交由 SQLite 打开时判断是否需要恢复，不手工删除。备份经过 VACUUM 整理后可能比原库小，不能只凭大小判断是否丢失数据。

需要人工恢复时，先完全停止脚本并关闭所有数据库连接；将现库以及其 `-journal` / `-wal` / `-shm` 文件一起移入单独的保留目录，再将选定的升级前备份恢复为 `assets/database.db`，不要混用现库的日志文件。回到旧版应用时必须同时恢复旧数据库；旧程序不能直接读取 v2。

同时保留当前 `credentials.json`；若选定数据库备份有配套凭据备份，应将其一并恢复为 `assets/credentials.json`。旧版数据库备份本身仍包含凭据时，迁移会以旧库中的值为准重新生成对应文件内容。

备份与恢复机制参考：[SQLite VACUUM INTO](https://www.sqlite.org/lang_vacuum.html)、[SQLite 回滚日志](https://www.sqlite.org/fileformat2.html#the_rollback_journal)。

## Worker 同步实现

- `src/sync/domain.ts` 固定 14 张业务实体表的字段白名单，与 Worker 的 schema 3 对齐；`config`、网站标签缓存、下载任务和凭据不上传。图库标签、搜索词折叠为父实体 JSON；下载时规范化顺序与缺省值再写回附属表。
- DBManager 首次打开时在事务内创建 `sync_control`、`sync_meta`、`sync_dirty`、`sync_shadow`、`sync_stage`、`sync_conflicts` 及变更触发器。它们是本机同步状态，不加入云端白名单。触发器与业务写入同事务提交，父记录和附属表变化均进入队列；远端写入通过同事务内的 applying 标记抑制回传。
- 连接时验证密钥、绑定本机设备并校验服务版本。连接文件与同步状态沿用凭据修订号协议一起提交。切换服务会丢弃旧服务的游标、影子和待重试请求，保留本机业务数据，清除其他设备的旧计数缓存，然后重新合并。
- 首次同步先以 `VACUUM INTO` 创建 `database.db.before-sync-<UUID>.db`。分页快照和封存前变更先保存到暂存表，完整合并与游标一起提交。会话 request_id、分页位置、seal/complete 阶段均落盘，响应丢失后能继续重试。过期游标触发重新完整下载。
- 上传按依赖排序，每批最多 8 条且控制在 128 KiB 请求内。batch_id、op_id、提交前快照和队列修订号先落盘。超时复用原批次；成功仅清除对应旧修订，保留请求期间发生的新修改。增量游标只在整段下载完成并应用后推进。
- 三方合并比较本机、已同步影子和云端：不同字段的修改可合并，同字段不同值或编辑与删除冲突由用户选择；阅读页码按最近访问时间选择，访问时间取最近、首次时间取最早。未处理的冲突保留，不阻塞无关记录。标签计数仅上传本设备分量，下载分量取 MAX，查询按词求和。
- AI / WebDAV 选中状态保持唯一，先取消旧选中项再提交新项。WebDAV 定义可同步；本机缺少对应凭据条目时不作为当前服务使用，在 WebDAV 设置中确认匿名访问或填写认证信息后启用。
- 自动同步默认关闭；开启后在前台每 30 秒检查，正常轮次间隔至少 60 秒，失败退避，恢复前台时检查。JSBox 关闭时不承诺后台同步。管理页支持暂停、冲突处理、设备重命名/解绑和断开连接。

执行 `npm run check:sync` 验证客户端与 Worker 处理代码的联调，包含现有数据库回归检查。测试使用 Node.js 22.13+ 的真实 SQLite，并用适配层替代 D1 binding；默认读取同级 `cloudflare-d1-sync`，也可设置 `SYNC_WORKER_PATH`。缺少 Worker 项目时联调用例会明确标为跳过。此测试不替代 workerd 或 JSBox 真机验证。
